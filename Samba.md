# Samba installation and configuration

```sudo apt-get install samba```

```sudo systemctl --type=service```

*smbd.service* started automatically


```sudo zfs set sharesmb=on depot/silo```

```sudo zfs share depot/silo```
